#%Module

proc ModulesHelp { } {
    puts stderr {   The X protocol C-language Binding (XCB) is a replacement for Xlib featuring a small footprint,
latency hiding, direct access to the protocol, improved threading support, and extensibility. - Homepage: http://xcb.freedesktop.org/
    }
}

module-whatis {Description: The X protocol C-language Binding (XCB) is a replacement for Xlib featuring a small footprint,
latency hiding, direct access to the protocol, improved threading support, and extensibility. - Homepage: http://xcb.freedesktop.org/}

set root    /apps/leuven/thinking/2015a/software/libxcb/1.11-foss-2015a-Python-2.7.9

conflict libxcb

if { ![is-loaded foss/2015a] } {
    module load foss/2015a
}

if { ![is-loaded Python/2.7.9-foss-2015a] } {
    module load Python/2.7.9-foss-2015a
}

if { ![is-loaded xcb-proto/1.11-foss-2015a-Python-2.7.9] } {
    module load xcb-proto/1.11-foss-2015a-Python-2.7.9
}

if { ![is-loaded libXau/1.0.8-foss-2015a] } {
    module load libXau/1.0.8-foss-2015a
}

if { ![is-loaded libpthread-stubs/0.3-foss-2015a] } {
    module load libpthread-stubs/0.3-foss-2015a
}

prepend-path	CPATH		$root/include
prepend-path	LD_LIBRARY_PATH		$root/lib
prepend-path	LIBRARY_PATH		$root/lib
prepend-path	MANPATH		$root/share/man
prepend-path	PKG_CONFIG_PATH		$root/lib/pkgconfig

setenv	EBROOTLIBXCB		"$root"
setenv	EBVERSIONLIBXCB		"1.11"
setenv	EBDEVELLIBXCB		"$root/easybuild/libxcb-1.11-foss-2015a-Python-2.7.9-easybuild-devel"


# Built with EasyBuild version 1.16.1
